import { Navbar } from "react-bootstrap";

const Header = () => (
  <Navbar bg="info" variant="light" expand="lg" className="justify-content-center text-black">
    <Navbar.Brand className="text-center">
      Header
    </Navbar.Brand>
  </Navbar>
);

export default Header;
